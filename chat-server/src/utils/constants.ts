export enum Routes {
  AUTH = 'auth',
  USER = 'user',
  CONVERSATION = 'conversation',
}
export enum Services {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USERS_SERVICE',
  CONVERSATION = 'CONVERSATION_SERVICE',
}

export const ONE_DAY = 60000 * 60 * 24;
